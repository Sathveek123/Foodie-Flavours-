{
  "buildCommand": "cd flavora-kitchen && npm install && npm run build",
  "outputDirectory": "flavora-kitchen/dist",
  "installCommand": "cd flavora-kitchen && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
